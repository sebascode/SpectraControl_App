# Go
backend/spectractl
backend/spectractl.exe

# Rust / Tauri
src-tauri/target/

# Bridge config (contains API key)
backend/.hue_config
.hue_config

# Python (legacy, mantenido por si acaso)
__pycache__/
*.py[oc]
.venv
